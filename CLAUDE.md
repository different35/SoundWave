# SoundWave - Agent Constitution & Rules

## ⚠️ CRITICAL COMMAND CONSTRAINTS (NEVER VIOLATE)
1. NO AUTOMATIC COMMITS: You are strictly FORBIDDEN from running `git commit`, `git push`, or any git write commands autonomously.
2. NO CODE EXECUTION WITHOUT PERMISSION: You must present all code modifications in the terminal and wait for explicit user approval before proceeding.
3. PAUSE AND PROMPT: If code changes are completed, output the recommended git command as a text snippet for the user to copy-paste manually.

## 🛠️ ENVIRONMENT & TECH STACK
- OS Platform: Android / Termux (Linux environment)
- Build Tool: npm (Do not use yarn/pnpm unless specified)
- Current Active Plan: Referenced in `PLAN.md`

## 📋 KANBAN INTEGRATION RULE
- ALWAYS use the integrated task tools (e.g., `TodoWrite`) to broadcast sub-tasks to the `claude-code-kanban` server (`http://localhost:3456`) before initiating any code implementation.

